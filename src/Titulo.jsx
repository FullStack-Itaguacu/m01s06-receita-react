/**
 * Este componente foi criado durante a aula,
 * mas não está sendo utilizado no App.jsx!
 */
const Titulo = (props) => {
  console.log(props);

  return <h1>{props.lista[props.indice]}</h1>;
};

export default Titulo;
